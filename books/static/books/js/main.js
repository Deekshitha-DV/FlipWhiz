$(document).ready(function() {
    // Initialize the flipbook
    $('.flipbook').turn({
        width: 800,
        height: 600,
        elevation: 50,
        gradients: true,
        autoCenter: true
    });
});